
def square_and_sum(x,y) :
    x= x*x#Square x
    return y + x #Add y and return

import random

def random_list(n):
    """ Returns a list of length n with random floats. Returns empty list for negative n. """
    new lst = []
    for _ in range(n):
       new_lst += random.randomint()
    return new_lst

def positive_num_to_string(n):
    n = str(n)
    if n <= 0 then:
        return n
    else :
        raise Negative number!

